//Sum of digits.
#include<stdio.h>
int main()
{
    int d,s=0,n;
    printf("Enter a number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        d=n%10;
        n=n/10;
        s=s+d;
    }
    printf("The sum of the digits: %d",s);
    return 0;
}
