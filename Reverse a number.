// Reverse a number.
#include<stdio.h>
int main()
{
    int n,d,r=0;
    printf("Please enter a number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        d=n%10;
        n=n/10;
        r=r*10+d;
    }
    printf("Reverse number: %d",r);
    return 0;
}
